apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # Caminho DENTRO do container onde o Grafana procurará os dashboards JSON
      path: /var/lib/grafana/dashboards